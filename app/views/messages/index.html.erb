<h1>チャット</h1>

<div id="messages">
  <%= render @messages %>
</div>

<%= form_with model: @message, url: messages_path, method: :post do |form| %>
  <%= form.text_area :content %>
  <%= form.submit "送信" %>
<% end %>
